// Template — copy this to secrets.h (gitignored) in the same folder and
// fill in real values. dial_node.ino #includes "secrets.h", not this file.

#define WIFI_SSID_SECRET     "YOUR_WIFI_SSID"
#define WIFI_PASSWORD_SECRET "YOUR_WIFI_PASSWORD"
